#include <stdio.h>
#include "functions.h"

int main() {

    int num1, num2, gcd;

    a, b, s;
  
  // ENTRADA DE DADOS
  scanf("%d %d" , &num1, &num2);

  // PROCESSAMENTO - EXECUTANDO A FUNCAO
  gcd = mdc(int x, int y);

  // SAIDA - IMPRIMINDO O RESULTADO
  printf("MDC = %d\n", gcd);

  return(0);
}
// 18 60
// MDC = 6